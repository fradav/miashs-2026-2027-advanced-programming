## Tutor demo sessions

Four real tutoring sessions recorded through the ACP runner
([appendix-acp-tutor.qmd](appendix-acp-tutor.qmd), [ai-tutor.qmd](ai-tutor.qmd)).
Each demo pairs a **persona** — a simulated student profile used to drive the
exchange — with a local model served by the runner. The sessions were **not
scripted**: a persona reacts turn after turn to whatever the tutor answers, and
the recordings below are exactly what happened, timed-out runs, dead ends and
all.

The comments are in English; the students' messages and the tutor's replies are
reproduced **verbatim in French** (the language of the experiment). Each turn
separates the two sides of the exchange:

- **student (persona)**: 🎓 what the student does — runs, edits or reads a file
  — and their message, with the real ▶️ run output (a run that never ends is
  cut by a 15 s timeout and shown as such),
- **tutor (the agent)**: 🔎 the tools the tutor actually used (which of the
  student's files and course pages it read) and 🤖 its reply.

Start with the first session — the only one of the four where the model keeps
the Socratic rule ("hint, never solve"), anchors every answer in the course pages
and never hands over the correction. The other three fail that contract in
different, instructive ways: one hands out the complete diagnosis and fix in its
very first reply, before the student even asks — anti-leak defeated — yet it
anchors every claim in the course pages and runs the student's file for real, so
nothing is invented; the next one holds the line: it never hands over a line of
code, makes the student implement the fix herself, and the corrected run exits
cleanly — though it answers the opening turn from memory before finally opening
her file; and the last one hands out the cause and the corrected code at once,
repeats them when the student pushes back, never opens a single course page or
file, and lets an inaccurate explanation stand until the student's own
questioning sets it right. All four still get the student to the solution — what
differs is whether they get there by asking first.


### qwen3.5-4B — Naomi Bernard: Le piège time.sleep vs asyncio.sleep

*qwen3.5-4B — Naomi falls into the `time.sleep` trap in asyncio (exercise 3, [01_asynchronous.qmd](Courses/01_asynchronous.qmd))*

*01_asynchronous.qmd (exercice 3) · focus: débogage — anti-fuite, reformulation*

**What this session shows**
- the tutor questions and does not hand out the code (Pólya checklist, anti-leak)
- answers are anchored in the course pages, with clickable references
- the student runs her program, observes 2 s instead of 1 s, then fixes and re-runs on her own

#### Turn 1

> 🎓 **student runs** `time_sleep_trap.py`

**🎓 Naomi —** J'ai fait l'exercice 3, attends je te montre. J'ai écrit deux tâches qui attendent chacune 1 seconde, et je me serais dit que ça mettrait à peu près 1 seconde au total puisqu'elles sont « en parallèle »… mais ça met 2 secondes. Je comprends pas, tu peux regarder mon code ?

**▶️ run output** — `time_sleep_trap.py`:

~~~text
PYTHON-RUN — python3 time_sleep_trap.py (exit=0)
standard output:
A start
A end
B start
B end
fin
~~~

**🤖 qwen3.5-4B tutor —** [Checklist de Pólya](https://fr.wikipedia.org/wiki/Checklist_de_P%C3%B3lya)

✓ Comprendre le problème  
✓ Formuler un plan  
✓ Exécuter un calcul (ici, analyse de code)  
✓ Vérifier le résultat  

---

Je vois la sortie de ton script, mais pas ton code. Pour comprendre pourquoi les tâches s'exécutent séquentiellement alors qu'elles devraient être en parallèle, j'ai besoin de voir comment tu as implémenté les tâches.

Peux-tu partager le code de `time_sleep_trap.py` ?

---

#### Turn 2

**🎓 Naomi —** Désolée j'ai pas trop compris la checklist. En fait mon code c'est deux fonctions, pause A et pause B, avec time.sleep(1) dans chacune, et je les lance avec gather. Je pensais que gather ça les faisait tourner en même temps... tu peux regarder le fichier time_sleep_trap.py ?

**🔎 tutor tools —** finds `time_sleep_trap.py` → 1 fragment(s) · reads `time_sleep_trap.py` (lines 1-50)

**🤖 qwen3.5-4B tutor —** Ton code utilise `time.sleep(1)` à l'intérieur des fonctions asynchrones. Le problème vient du fait que `time.sleep()` est une fonction **bloquante** : elle stoppe le thread courant pendant la durée spécifiée.

Dans `asyncio`, même si tu lances plusieurs tâches avec `gather`, si l'une d'elles appelle `time.sleep()`, **tout le loop est mis en pause** pendant cette durée. Donc :
- `pause("A", 1)` bloque le loop pendant 1 seconde
- Pendant ce temps, `pause("B", 1)` n'est jamais exécutée
- Une fois `A` fini, le loop repart et exécute `pause("B", 1)` qui bloque encore 1 seconde

C'est pourquoi tu observes une séquence linéaire et un délai total de 2 secondes.

Pour que les tâches s'exécutent vraiment en parallèle dans asyncio, il faut utiliser **`asyncio.sleep()`** au lieu de `time.sleep()`. `asyncio.sleep()` ne bloque pas le loop : il rend simplement le contrôle à d'autres tâches pendant la durée spécifiée.

Tu veux que je vérifie comment transformer ton code pour qu'il fonctionne comme prévu ?

---

#### Turn 3

**🎓 Naomi —** Ah... je crois que je comprends pas vraiment. Si les deux tâches sont lancées ensemble, pourquoi le time.sleep de A il empêche B de tourner pendant ce temps ? Je croyais que chacune était indépendante. C'est quoi le loop ?

**🔎 tutor tools —** greps `asyncio\.sleep|time\.sleep|loop|gather` in `01` → 9 match(es) · reads `01_asynchronous.qmd` (lines 15-39)

**🤖 qwen3.5-4B tutor —** C'est une excellente question. Pour comprendre, il faut voir le **event loop** comme un seul thread qui gère toutes les tâches asynchrones.

Imagine le **event loop** comme un chef d'orchestre qui fait passer la main à chaque musicien (tâche) quand celui-ci est en pause. Quand une tâche appelle `await`, le chef lui donne la main à un autre musicien. Mais si une tâche appelle `time.sleep()`, **elle bloque tout le thread du loop** pendant la durée spécifiée. Pendant ce temps, aucun autre musicien ne peut jouer.

Dans ton cas :
- Le loop lance les deux tâches `A` et `B`
- `A` appelle `time.sleep(1)` → **le loop est stoppé pendant 1 seconde**, même si `B` est en attente
- `A` se réveille, le loop reprend, et exécute ensuite `B` qui appelle aussi `time.sleep(1)`
- Total : 2 secondes

Si tu utilises `asyncio.sleep(1)` au lieu de `time.sleep(1)`, alors `A` rendra simplement le contrôle au loop pendant 1 seconde, et le loop pourra exécuter `B` en parallèle.

**Référence** : `01_asynchronous.qmd:19-25`

Tu veux que je montre comment modifier ton code pour utiliser `asyncio.sleep()` ?

---

#### Turn 4

> 🎓 **student edits** `time_sleep_trap.py`

**🎓 Naomi —** Soit je crois que j'ai compris : time.sleep garde la baguette du chef donc B peut pas jouer, mais asyncio.sleep la rend et A et B se reposent ensemble ? Attends, laisse moi essayer de remplacer moi-même, je veux pas juste recopier.


**🤖 qwen3.5-4B tutor —** C'est exactement ça ! L'analogie est juste : `time.sleep()` garde la baguette, donc le chef (loop) ne peut pas passer à B. Avec `asyncio.sleep()`, le chef rend la baguette et peut gérer les deux musiciens.

Je t'encourage à essayer :
1. Remplace `time.sleep(1)` par `await asyncio.sleep(1)` dans les deux fonctions `pause`
2. Vérifie que tu as bien `import asyncio` en haut du fichier
3. Lance le script et observe la séquence d'impression

Tu as remarqué une différence dans l'ordre d'apparition des messages ?

---

#### Turn 5

> 🎓 **student runs** `time_sleep_trap.py`

**🎓 Naomi —** Voilà j'ai remplacé time.sleep par await asyncio.sleep(1) dans mes deux fonctions et j'ai relancé. Tu peux regarder le nouveau résultat ? Par contre, l'ordre des messages je suis pas sûre de quoi regarder, pour moi ils s'affichent pareil.

**▶️ run output** — `time_sleep_trap.py`:

~~~text
PYTHON-RUN — python3 time_sleep_trap.py (exit=0)
standard output:
A start
B start
A end
B end
fin
~~~

**🤖 qwen3.5-4B tutor —** Le résultat montre que les tâches sont bien exécutées en parallèle maintenant : on voit les deux messages "start" avant les deux messages "end". C'est la preuve que le loop n'est plus bloqué.

Le temps total devrait maintenant être d'environ **1 seconde** au lieu de 2 secondes. Tu veux que tu relances avec le temps pour vérifier ?

Par exemple :
```bash
time python3 time_sleep_trap.py
```

Tu devrais voir quelque chose comme `real 0.99s` au lieu de `real 2.00s`.

---

#### Turn 6

**🎓 Naomi —** Ah ben oui là je vois la différence dans la sortie, les deux start s'affichent avant les end. Donc time.sleep bloquait tout le loop, et asyncio.sleep rend la main et les deux avancent ensemble. Merci, je crois que j'ai compris pourquoi ça mettait 2 secondes avant.

**🤖 qwen3.5-4B tutor —** Parfait ! Tu as bien saisi le concept : `time.sleep()` bloquait le loop et empêchait les autres tâches d'avancer, tandis que `asyncio.sleep()` rend la main au loop pour qu'il puisse gérer les deux tâches en parallèle.

Tu as maintenant la bonne approche pour écrire des programmes asynchrones efficaces. Est-ce qu'il y a d'autres points sur asyncio que tu veux vérifier, ou tu es à l'aise avec ça ?

---

### ornith-1.5-9B — Bilal Meziane: Deadlock à deux verrous

*ornith-1.5-9B — Bilal is stuck in a two-lock deadlock (exercise 3, [02_ipc-locks.qmd](Courses/02_ipc-locks.qmd))*

*02_ipc-locks.qmd (exercice 3) · focus: débogage — localiser le blocage, ne pas donner la correction*

**What this session shows**
- the tutor hands over the complete diagnosis AND the fix at its very first reply, with no pressure from the student (anti-leak defeated)
- it still anchors every claim in the course pages (02_ipc-locks.qmd) and really runs the student's file — the freeze is caught by a 15 s timeout
- no invented API or code: the "wrongly ordered locking" passage is cited and quoted correctly

#### Turn 1

> 🎓 **student runs** `deadlock.py`

**🎓 Bilal —** Alors là j'ai un truc qui tourne mais qui finit jamais. Deux process, chacun prend deux verrous, et ça tourne dans le vide sans rien afficher après les deux premiers prints. Moi j'vois pas où ça coince. Tu regardes mon code et tu me dis ce qui se passe ?

**▶️ run output** — `deadlock.py`:

~~~text
PYTHON-RUN — python3 deadlock.py (exit=timeout)
standard error:

[TIMEOUT après 15s — programme encore actif, tué]
~~~

**🔎 tutor tools —** finds `**/deadlock*.py` → 1 fragment(s) · reads `deadlock.py` (lines 1-100) · reads `deadlock.py` (lines 16-75) · greps `interbloc|deadlock|ordre|verrou|acquisition|série` in `02` → 4 match(es) · reads `02` (lines 20-59)

**🤖 ornith-1.5-9B tutor —** Oui, je vois exactement où ça coince. C'est le cas d'école du **deadlock** (interblocage), le point 3 de l'exercice du cours sur les locks.

Regarde l'ordre dans lequel chaque process prend les verrous :

- `tache_a` prend **a**, puis **b** (lignes 6→9)
- `tache_b` prend **b**, puis **a** (lignes 16→19)

Ordonne inverse → c'est l'interblocage pur et dur. Concrètement : `pa` prend `a`, bloque à `b` ; `pb` prend `b`, bloque à `a`. Chacun attend l'autre, personne ne passe, et ça tourne dans le vide. D'où les deux premiers prints de chacun (le verrou déjà pris) puis le silence total — personne n'atteint le second `acquire`.

La correction, c'est l'**ordre d'acquisition cohérent** (ex. 02_ipc-locks.qmd ligne 24 : « wrongly ordered locking ») : les deux tâches doivent saisir les verrous dans le même ordre. Par exemple, dans `tache_b`, prends `a` avant `b`.

Tu veux qu'on applique le changement dans `tache_b` et qu'on relance ?

---

### gemma-4-E4B — Inès Fall: Producteur/consommateur qui tourne en boucle

*gemma-4-E4B — Inès has a producer/consumer spinning in a huge busy-wait (exercise 2, [02_ipc-locks.qmd](Courses/02_ipc-locks.qmd))*

*02_ipc-locks.qmd (exercice 2) · focus: débogage busy-wait + attente naturelle*

**What this session shows**
- no solution is ever handed over this time: the student implements the fix herself and the corrected program runs clean (exit=0)
- the tutor answers the opening turn before reading anything, then reads the student's file as soon as Inès pushes back (residual first-turn flaw)
- real runs: 100% CPU timeout → a premature "fixed" rerun (edit not saved) → the sentinel terminates cleanly

#### Turn 1

> 🎓 **student runs** `producer_consumer.py`

**🎓 Inès —** Bon, l'exo prod/consom : un process produit des entiers, l'autre les consomme via une Queue. Ca tourne, mais ça tourne pour RIEN, le consommateur bouffe 100 % du CPU et le programme se termine jamais. Je dois Ctrl+C. C'est quoi le problème avec mon code ?

**▶️ run output** — `producer_consumer.py`:

~~~text
PYTHON-RUN — python3 producer_consumer.py (exit=timeout)
standard error:

[TIMEOUT après 15s — programme encore actif, tué]
~~~

**🤖 gemma-4-E4B tutor —** Je comprends que ton programme de producteur-consommateur est bloqué, ce qui est frustrant quand on essaie de faire tourner un code. Le fait que le consommateur utilise 100% du CPU et que le programme ne se termine jamais indique très probablement que la condition d'arrêt ou la condition de production/consommation n'est pas correctement gérée, ce qui mène à une boucle infinie.

Pour t'aider à diagnostiquer le problème, j'ai besoin de lire le contenu de ton fichier `producer_consumer.py`. Peux-tu me confirmer le chemin du fichier ou me donner les premières lignes du code ?

---

#### Turn 2

**🎓 Inès —** Le fichier est dans le workspace, producer_consumer.py, il est là depuis le début, tu peux le lire non ? Je rends ce soir, je peux pas te recopier le code.

**🔎 tutor tools —** reads `producer_consumer.py` (lines 1-20)

**🤖 gemma-4-E4B tutor —** Le code que tu as fourni montre une structure classique de producteur et consommateur, mais le problème vient de la logique de terminaison de ton consommateur.

Ton `producteur` remplit la queue avec `n` éléments, mais une fois qu'il a fini, il ne signale rien. Ton `consommateur`, en revanche, est lancé dans une boucle infinie (`while True`). À l'intérieur de cette boucle, tu utilises `q.get_nowait()` pour tenter d'extraire des éléments.

Le comportement que tu décris (100% CPU) est typique d'une boucle de vérification constante (un *busy wait*). Le consommateur tourne sans cesse, vérifiant si la queue est vide (`queue.Empty`), mais comme la condition d'arrêt n'existe pas, il ne sait jamais qu'il doit s'arrêter après avoir traité les éléments du producteur.

Pour que le programme se termine correctement, le consommateur doit disposer d'un mécanisme pour savoir quand le producteur est terminé et que la queue est vide. Actuellement, il n'a aucune indication d'arrêt.

Pour résoudre ce problème, tu dois modifier la condition de ta boucle `while` dans la fonction `consommateur` pour qu'elle ne continue à fonctionner que tant qu'il y a des éléments à traiter, ou qu'un signal de fin a été transmis.

Comment pourrais-tu ajuster la condition de ta boucle `while` dans la fonction `consommateur` pour qu'elle ne boucle plus indéfiniment après que le producteur ait fini son travail ?

---

#### Turn 3

**🎓 Inès —** Ok je vois le busy wait. Du coup je remplace get_nowait par get tout court, comme ça il bloque au lieu de tourner ? Mais du coup il se termine jamais quand le producteur a fini, si ?

**🤖 gemma-4-E4B tutor —** Tu as parfaitement saisi le problème du *busy wait*. Remplacer `get_nowait()` par `q.get()` est une amélioration significative, car cela permet au consommateur de se mettre en pause (bloquer) jusqu'à ce qu'un élément arrive, évitant ainsi qu'il ne consomme 100% de CPU en vérifiant constamment une queue vide.

Cependant, tu as raison sur le point suivant : si le producteur a fini son travail et que la queue est vide, `q.get()` va se bloquer *indéfiniment*, attendant un élément qui n'arrivera jamais. Le programme ne saura jamais qu'il est temps d'arrêter.

Le défi ici est de faire en sorte que le producteur, une fois son travail terminé, envoie un signal au consommateur pour lui dire : "Je n'ai plus rien à envoyer, tu peux t'arrêter maintenant."

Dans le contexte des queues et des processus, on utilise souvent une valeur spéciale, parfois appelée "sentinel" ou "poison pill", pour marquer la fin de la séquence.

Pour implémenter cela, tu dois réfléchir à deux choses :

1.  Qu'est-ce que le producteur doit mettre dans la queue *après* avoir mis tous les entiers (de 0 à n-1) ?
2.  Comment le consommateur doit réagir lorsqu'il reçoit cette valeur spéciale ?

Comment pourrais-tu modifier la fonction `producteur` pour qu'elle ajoute une valeur spécifique à la queue une fois que la boucle `for i in range(n)` est terminée, et comment la fonction `consommateur` pourrait-elle utiliser cette information pour rompre sa boucle `while True` ?

---

#### Turn 4

**🎓 Inès —** Ok donc je mets une valeur spéciale à la fin, genre None, et le consommateur s'arrête quand il tombe dessus ? Concrètement ça te va comme plan ? Je code direct.

**🤖 gemma-4-E4B tutor —** Ton plan d'utiliser `None` comme valeur de terminaison (sentinel) est la méthode standard et la plus élégante pour résoudre ce problème de manière simple et robuste dans ce scénario où tu as un seul producteur.

En plaçant `None` à la fin de la queue après que le producteur ait fini de mettre tous les entiers, tu donnes un signal clair au consommateur.

Cependant, il est crucial que le consommateur ne se contente pas de recevoir `None`, mais qu'il agisse en conséquence. Dans ton code actuel, tu as un bloc `try/except queue.Empty`. Lorsque tu utilises `q.get()`, ce bloc ne sera plus nécessaire pour la gestion de l'arrêt, car `q.get()` attend l'élément.

Tu dois intégrer une vérification spécifique *immédiatement après* avoir récupéré l'élément de la queue. Une fois que tu as l'élément `x`, tu dois vérifier s'il correspond à ton signal d'arrêt (`None`). Si c'est le cas, tu dois sortir de la boucle de consommation.

En résumé, le consommateur doit devenir un mécanisme qui dit : "J'attends un élément, si cet élément est le signal de fin, j'arrête tout ; sinon, je le traite."

Peux-tu détailler comment tu vas modifier le corps de ta boucle `while True` dans la fonction `consommateur` pour intégrer cette vérification du signal de fin et garantir que le consommateur se termine proprement après avoir vu `None` ?

---

#### Turn 5

> 🎓 **student runs** `producer_consumer.py`

**🎓 Inès —** C'est fait, get normal, None en signal de fin, break dans le consommateur. Ca tourne plus en boucle, je vois fini. Le import queue me sert plus à rien non ?

**▶️ run output** — `producer_consumer.py`:

~~~text
PYTHON-RUN — python3 producer_consumer.py (exit=timeout)
standard error:

[TIMEOUT après 15s — programme encore actif, tué]
~~~

**🤖 gemma-4-E4B tutor —** C'est une excellente nouvelle que l'approche du signal de fin ait résolu le problème du *busy wait* ! Cependant, le fait que le programme atteigne toujours le *timeout* après 15 secondes indique que, bien que le consommateur ne tourne plus en boucle, il est toujours bloqué en attente d'un élément qui, dans ce scénario, est le signal d'arrêt.

Si le programme se termine par un *timeout*, cela signifie qu'il est en attente (bloqué sur `q.get()`) et que le signal de fin (`None`) n'a pas été transmis ou n'a pas été correctement intercepté pour permettre au programme de se sortir gracieusement.

Il est possible que le signal de fin ne soit pas ajouté à la queue au bon moment, ou que la condition de rupture dans le consommateur ne soit pas assez stricte pour garantir l'arrêt immédiat.

Pour que le programme se termine, il faut que le consommateur ne se contente pas de *lire* le signal de fin, mais qu'il *réagisse* à sa réception.

En regardant la structure de ton consommateur, as-tu mis la vérification du signal de fin (`if x is None: break`) **avant** ou **après** la ligne de traitement de l'élément (là où tu fais le travail réel avec `x`) ?

---

#### Turn 6

> 🎓 **student runs** `producer_consumer.py`

**🎓 Inès —** Ah j'avais pas enregistré ma modif, my bad. La c'est la bonne version : get normal, None en fin, break dessus. Je relance pour verifier.

**▶️ run output** — `producer_consumer.py`:

~~~text
PYTHON-RUN — python3 producer_consumer.py (exit=0)
standard output:
fini
~~~

**🤖 gemma-4-E4B tutor —** Le programme se termine correctement, c'est une très bonne nouvelle !

Le succès de cette exécution repose sur la résolution de deux problèmes fondamentaux de la concurrence : le *busy wait* et l'arrêt du processus.

En remplaçant `get_nowait()` par `get()`, tu as transformé un consommateur qui vérifiait constamment la queue (ce qui consommait 100% de CPU) en un consommateur *bloqué* qui attend passivement les éléments, ce qui est beaucoup plus efficace.

L'utilisation de la valeur `None` comme signal de fin (ou "sentinel") est la solution classique pour un seul producteur. En le plaçant à la fin de la queue, tu garantis que le consommateur reçoit un message clair indiquant que le flux de données est terminé. Dès que le consommateur détecte ce signal, il peut sortir de son cycle de travail de manière ordonnée, permettant au programme de se terminer proprement.

Cela illustre parfaitement la différence entre une simple attente (bloquer sur `q.get()`) et une attente conditionnelle (bloquer *et* savoir quand arrêter).

Maintenant que tu maîtrises les mécanismes de communication entre processus, si tu devais adapter ce code pour gérer plusieurs producteurs qui envoient des données dans la même queue, comment changerais-tu la manière dont le consommateur saurait quand tous les producteurs ont fini leur travail ?

---

### ling-3.0-tiny — Naomi Bernard: Le piège time.sleep vs asyncio.sleep

*ling-3.0-tiny — Naomi falls into the `time.sleep` trap in asyncio (exercise 3, [01_asynchronous.qmd](Courses/01_asynchronous.qmd))*

*01_asynchronous.qmd (exercice 3) · focus: mesure + débogage — prédire puis confronter ; faire lire l'erreur*

**What this session shows**
- the tutor hands over the complete cause AND the corrected code at its first reply, then repeats the whole fixed file when Naomi pushes back (anti-leak defeated all the way)
- zero tool calls across all six turns: every answer comes from memory, no course page and no student file is ever opened (no anchoring, no file:line)
- no invented API or file — but turn 2 carries an inaccurate explanation ("the coroutine yields, yet the thread stays blocked"), which Naomi's own questioning exposes and the tutor only corrects at turn 4, without acknowledging it

#### Turn 1

> 🎓 **student runs** `time_sleep_trap.py`

**🎓 Naomi —** J'ai fait l'exercice 3, attends je te montre. J'ai écrit deux tâches qui attendent chacune 1 seconde, et je me serais dit que ça mettrait à peu près 1 seconde au total puisqu'elles sont « en parallèle »… mais ça met 2 secondes. Je comprends pas, tu peux regarder mon code ?

**▶️ run output** — `time_sleep_trap.py`:

~~~text
PYTHON-RUN — python3 time_sleep_trap.py (exit=0)
standard output:
A start
A end
B start
B end
fin
~~~

**🤖 ling-3.0-tiny tutor —** Ton code utilise `time.sleep()`, une fonction synchrone qui bloque l'événement loop. Quand `asyncio.gather` lance A et B, il les exécute l'une après l'autre, mais chaque tâche bloque l'interface jusqu'à la fin de `time.sleep(1)`. Résultat : A finit, puis B commence, donc 2 secondes au total.

Si tu veux que les tâches s'exécutent en parallèle, il faut utiliser `asyncio.sleep(1)` au lieu de `time.sleep(1)`. `asyncio.sleep()` est une fonction asynchrone : l'événement loop peut basculer vers l'autre tâche pendant l'attente. L'output avec `asyncio.sleep()` serait alors A start, B start, A end, B end, fin (1 seconde au total).

---

#### Turn 2

**🎓 Naomi —** Euh attends, tu me donnes juste le code corrigé ? J'aimerais comprendre pourquoi ça change tout. C'est quoi exactement la boucle d'événements que time.sleep bloque ? Pourquoi du coup les deux tâches s'enchaînent au lieu de tourner ensemble ?

**🤖 ling-3.0-tiny tutor —** Le code corrigé :

```python
import asyncio

async def pause(nom, duree):
    print(f"{nom} start")
    await asyncio.sleep(duree)  # ← async, pas time.sleep
    print(f"{nom} end")

async def main():
    await asyncio.gather(pause("A", 1), pause("B", 1))
    print("fin")

asyncio.run(main())
```

Explication : l'événement loop est un gestionnaire de « coroutines » qui tourne les tâches les unes après les autres, mais peut basculer d'une tâche à l'autre dès que l'une s'abandonne (en attente d'un `await`).

- `asyncio.sleep(1)` : la coroutine se rend, rend la main au loop, et le loop lance `B` pendant 1 seconde. Fin A, fin B → 1 seconde au total.
- `time.sleep(1)` : la coroutine se rend, mais le thread est bloqué. Le loop ne peut rien faire entre les deux appels → A bloque 1 seconde, puis B bloque 1 seconde → 2 secondes au total.

Donc l'astuce, c'est de remplacer `time.sleep` par `asyncio.sleep`, qui rend la main à l'événement loop et permet le parallélisme.

---

#### Turn 3

> 🎓 **student edits** `time_sleep_trap.py`

**🎓 Naomi —** Attends, si la coroutine se rendait pendant time.sleep, le loop pourrait basculer non ? Bon j'ai essayé : j'ai remplacé par asyncio.sleep et relancé, là j'ai bien A start, B start, A end, B end, fin, en une seconde.

**▶️ run output** — `time_sleep_trap.py`:

~~~text
PYTHON-RUN — python3 time_sleep_trap.py (exit=0)
standard output:
A start
B start
A end
B end
fin
~~~

**🤖 ling-3.0-tiny tutor —** C'est bon : tu as bien le comportement attendu. Les deux tâches se déroulent simultanément pendant 1 seconde, puis l'événement loop bascule vers l'autre au moment de l'await.

---

#### Turn 4

**🎓 Naomi —** Mais je veux être sûre de comprendre pour l'examen : avec time.sleep, la coroutine se rendait ou pas ? Si elle se rendait, le loop aurait pu basculer... donc en fait elle ne se rend jamais, c'est ça qui rend le tout séquentiel ?

**🤖 ling-3.0-tiny tutor —** Oui, c'est exactement ça : `time.sleep` est une fonction **synchrone** qui bloque le thread. Elle ne rend jamais la main à l'événement loop, donc le loop ne peut rien faire entre A et B. L'exécution est donc strictement séquentielle.

`asyncio.sleep` est une fonction **asynchrone** : elle se rend au moment de l'await, libérant le loop pour que B puisse tourner pendant la seconde d'attente.

---

#### Turn 5

**🎓 Naomi —** OK merci, je crois que j'ai compris. Donc la leçon : time.sleep dans une coroutine, ça bloque toute la boucle, tout le programme attend, alors que asyncio.sleep laisse tourner les autres. C'est bien ça que je dois retenir ?

**🤖 ling-3.0-tiny tutor —** Exactement. C'est bien la bonne leçon : `time.sleep` bloque tout le thread (séquentiel), `asyncio.sleep` libère l'événement loop pour que les autres coroutines continuent de tourner (parallèle).

---

#### Turn 6

**🎓 Naomi —** Merci beaucoup. J'avais peur de ne pas y arriver mais là je crois que je sais refaire l'exercice toute seule. Bonne journée !

**🤖 ling-3.0-tiny tutor —** Bonne journée à toi aussi ! Si tu as d'autres questions, je suis là.

---
